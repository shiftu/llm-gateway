apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: llm-gateway
  labels:
    app.kubernetes.io/name: llm-gateway
    app.kubernetes.io/managed-by: kustomize
spec:
  type: ClusterIP
  ports:
    - port: 7421
      targetPort: 7421
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: llm-gateway
