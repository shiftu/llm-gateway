apiVersion: v1
kind: Secret
metadata:
  name: llm-gateway-auth
  namespace: llm-gateway
  labels:
    app.kubernetes.io/name: llm-gateway
    app.kubernetes.io/managed-by: kustomize
type: Opaque
data:
  # Replace the value below with: echo -n 'YOUR_TOKEN' | base64
  # Current placeholder decodes to the string "CHANGE_ME"
  LLM_GATEWAY_TOKEN: Q0hBTkdFX01F
